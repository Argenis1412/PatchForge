## Problem

Why this change exists and what issue it resolves.

## Design

Architecture decisions, invariants affected, files touched.

## Verification

- `ruff check .` — clean
- `pytest <affected files>` — N passed

## Invariants Preserved

- List of key invariants that were intentionally NOT changed (or a note if none apply)
